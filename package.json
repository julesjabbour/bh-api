{
  "name": "bh-api",
  "version": "1.0.0",
  "private": true,
  "description": "BH Keyboard backend — espeak-ng IPA + Claude API proxy",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.0",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=20"
  }
}
